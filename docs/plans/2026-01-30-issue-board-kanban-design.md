# Issue Board Kanban 功能設計

## 概述

為 react-gantt-gitlab 專案新增 Issue Board (Kanban) 視圖，與現有 Gantt 視圖共用資料層，實現零延遲切換。

## 需求摘要

- 使用者可建立多個 Issue Board，每個 Board 包含多個 List
- 每個 List 依據 labels 篩選 issues（AND 邏輯）
- Issues 以卡片形式顯示，包含 ID、title、assignees、labels、tasks、due date
- 支援拖曳排序（同 List）和跨 List 移動（label 切換）
- 特殊 List：Others（未分類）、Closed（已關閉）
- 共用 Gantt 的篩選器和 project 選擇，篩選狀態完全同步
- 雙擊 issue 開啟 Editor，支援新增 issue
- Board 設定存於 GitLab Snippet，專案共享

---

## 架構設計

### 核心架構圖

```
┌─────────────────────────────────────────────────────────────┐
│                    GitLabDataContext                         │
│  (新建 shared context，從 GitLabGantt 抽出資料層)            │
│                                                              │
│  - tasks, links, milestones, epics                          │
│  - sync(), syncTask(), createTask(), deleteTask()           │
│  - filterOptions, activeServerFilters                       │
│  - provider, currentConfig, projectPath                     │
└─────────────────────────────────────────────────────────────┘
                           │
┌──────────────────────────┴──────────────────────────────────┐
│                    GitLabWorkspace                           │
│  (新建容器元件，管理 view 切換與共用 UI)                      │
│                                                              │
│  - ViewSwitcher (Gantt / Kanban)                            │
│  - ProjectSelector, FilterPanel (共用)                       │
│  - Editor panel (共用，雙擊 issue 開啟)                      │
│  - CreateItemDialog (共用)                                   │
└─────────────────────────────────────────────────────────────┘
            │                               │
  ┌─────────┴─────────┐         ┌───────────┴───────────┐
  │   GanttView       │         │    KanbanView         │
  │   (從現有抽出)     │         │    (新建)              │
  │                   │         │                       │
  │ - Gantt 圖表邏輯   │         │ - Board/List 管理     │
  │ - 拖曳/編輯處理    │         │ - 拖曳排序/跨 list    │
  └───────────────────┘         └───────────────────────┘
```

### 檔案結構

```
src/
├── contexts/
│   └── GitLabDataContext.tsx       # 共享資料 context
├── components/
│   ├── GitLabWorkspace/
│   │   ├── GitLabWorkspace.jsx     # 主容器（取代原 GitLabGantt 入口）
│   │   ├── GitLabWorkspace.css
│   │   └── ViewSwitcher.jsx        # Gantt/Kanban 切換
│   ├── GanttView/
│   │   ├── GanttView.jsx           # 從 GitLabGantt 抽出的 Gantt 視圖
│   │   └── GanttView.css
│   ├── KanbanView/
│   │   ├── KanbanView.jsx          # Kanban 主視圖
│   │   ├── KanbanView.css
│   │   ├── KanbanBoard.jsx         # Board 容器（lists + 拖曳）
│   │   ├── KanbanList.jsx          # 單一 List 欄位
│   │   ├── KanbanCard.jsx          # Issue 卡片
│   │   └── BoardSelector.jsx       # Board preset 選擇器
│   ├── Editor.jsx                  # (現有，共用)
│   └── shared/
│       └── ...
├── hooks/
│   └── useIssueBoard.ts            # Board preset CRUD hook
├── providers/
│   └── GitLabIssueBoardApi.ts      # Board Snippet API
└── types/
    └── issueBoard.ts               # Board 類型定義
```

### 共用元件

| 元件                | 說明                                       |
| ------------------- | ------------------------------------------ |
| `Editor`            | 雙擊 issue 開啟的側邊編輯面板              |
| `ProjectSelector`   | 專案/群組選擇                              |
| `FilterPanel`       | 篩選器（labels, milestones, assignees 等） |
| `CreateItemDialog`  | 新增 issue/milestone 對話框                |
| `CredentialManager` | 憑證管理                                   |

---

## 資料結構

### Board Preset 類型定義

```typescript
// types/issueBoard.ts

interface IssueBoardList {
  id: string; // UUID
  name: string; // 顯示名稱
  labels: string[]; // 篩選的 label 名稱（AND 邏輯）
  sortBy: 'position' | 'due_date' | 'created_at' | 'label_priority' | 'id';
  sortOrder: 'asc' | 'desc';
}

interface IssueBoard {
  id: string; // UUID
  name: string; // Board 名稱
  lists: IssueBoardList[]; // List 定義（順序即為顯示順序）
  showOthers: boolean; // 是否顯示 Others list
  showClosed: boolean; // 是否顯示 Closed list

  // 預留擴充
  metadata?: {
    createdAt?: string;
    updatedAt?: string;
    createdBy?: string;
    [key: string]: unknown;
  };
}

// Snippet 儲存格式（一個 snippet 存所有 boards）
interface IssueBoardStorage {
  version: 1;
  boards: IssueBoard[];
}
```

### Snippet 命名規則

- **Project**: `gantt-issue-boards-{projectId}`
- **Group**: `gantt-issue-boards-group-{groupId}`

### List 篩選邏輯

```typescript
// 判斷 issue 是否屬於某個 list
function issueMatchesList(issue: Task, list: IssueBoardList): boolean {
  // list.labels 內所有 label 都要存在於 issue（AND 邏輯）
  return list.labels.every((label) => issue.labels.includes(label));
}

// Others list：不符合任何自定義 list 的 issues
function isOthersIssue(issue: Task, lists: IssueBoardList[]): boolean {
  return !lists.some((list) => issueMatchesList(issue, list));
}
```

---

## Kanban Card 設計

### 卡片佈局

```
┌────────────────────────────────┐
│ #42 Issue title here that      │
│     might wrap to two lines    │
│ 👤 Alice, Bob +2               │
│ 🏷️ bug, urgent +3              │
│ ☑️ 3/5    -3d                  │
└────────────────────────────────┘
```

（實際使用 Font Awesome icons，非 unicode）

### 顯示欄位

| 欄位      | 來源                     | 顯示方式                                       |
| --------- | ------------------------ | ---------------------------------------------- |
| ID        | `issue.iid`              | `#42`                                          |
| Title     | `issue.text`             | 可換行，完整顯示                               |
| Assignees | `issue.assignees`        | 完整顯示，超過用 `+N`                          |
| Labels    | `issue.labels`           | 完整顯示，超過用 `+N`（沿用 grid column 邏輯） |
| Tasks     | `task_completion_status` | `3/5` 格式                                     |
| Due       | `due_date`               | `3d` / `-3d`（負數紅色）                       |

### 緊湊樣式

| 項目               | 設定    |
| ------------------ | ------- |
| 卡片 padding       | 6-8px   |
| 行距 (line-height) | 1.2-1.3 |
| 字體大小           | 12-13px |
| 卡片間距 (gap)     | 4-6px   |
| List 間距          | 8px     |

### Due Date 顯示規則

```typescript
function formatDueDate(dueDate: Date | null): {
  text: string;
  isOverdue: boolean;
} {
  if (!dueDate) return { text: '', isOverdue: false };

  const today = new Date();
  today.setHours(0, 0, 0, 0);
  const due = new Date(dueDate);
  due.setHours(0, 0, 0, 0);

  const diffDays = Math.ceil((due - today) / (1000 * 60 * 60 * 24));

  if (diffDays < 0) {
    return { text: `${Math.abs(diffDays)}d`, isOverdue: true }; // 紅色
  } else if (diffDays === 0) {
    return { text: 'today', isOverdue: false };
  } else {
    return { text: `${diffDays}d`, isOverdue: false };
  }
}
```

### 互動

- **Hover**: 顯示完整 title、所有 assignees、所有 labels 的 tooltip
- **雙擊**: 開啟 Editor 面板

---

## 拖曳互動

### 拖曳類型

| 操作             | 行為                                            | GitLab API                               |
| ---------------- | ----------------------------------------------- | ---------------------------------------- |
| 同 List 上下拖曳 | 更新 relative position                          | `PUT /issues/:iid/reorder`               |
| 跨 List 拖曳     | 移除來源 label + 加入目標 label + 更新 position | `PUT /issues/:iid` + reorder             |
| 拖曳到 Closed    | 關閉 issue                                      | `PUT /issues/:iid` (state_event: close)  |
| 從 Closed 拖出   | 重新開啟 + 加入目標 label                       | `PUT /issues/:iid` (state_event: reopen) |
| 拖曳到 Others    | 移除來源 label                                  | `PUT /issues/:iid`                       |

### 跨 List Label 處理

只處理來源 list 的 label：

- 從 List A（label: `bug`）拖到 List B（label: `feature`）
- 結果：移除 `bug`，加入 `feature`
- 其他 labels 保留不變

### 樂觀更新流程

```
1. 使用者拖曳放開
2. 立即更新本地 UI（樂觀更新）
3. 背景呼叫 GitLab API
4. 成功：不做額外動作
5. 失敗：回彈到原位置 + 顯示錯誤 toast
```

### 技術選型

使用 `@dnd-kit/core`：

- 支援 keyboard 拖曳（accessibility）
- 支援 touch 裝置
- 容易實作跨容器拖曳

### 拖曳視覺回饋

- 拖曳中：卡片不透明 + 陰影
- 放置目標：list 背景淡色高亮
- 無效目標：顯示禁止游標

---

## List 排序功能

### 排序選項

| 排序方式       | 欄位                | 說明                          |
| -------------- | ------------------- | ----------------------------- |
| Position       | `relative_position` | GitLab 原生排序（預設）       |
| Due Date       | `due_date`          | 依到期日                      |
| Created        | `created_at`        | 依建立時間                    |
| ID             | `iid`               | 依 issue 編號                 |
| Label Priority | label priority      | 依 GitLab label priority 設定 |

每個選項支援 Ascending / Descending。

### 排序 UI

每個 List header 有排序下拉選單：

```
┌─────────────────────────┐
│ In Progress        ▼   │  <- 點擊開啟選單
├─────────────────────────┤
│ Sort by: Position  ▼   │
│ ○ Ascending            │
│ ● Descending           │
│ ─────────────────────  │
│ [Apply to GitLab]      │  <- 警告按鈕
└─────────────────────────┘
```

（使用 Font Awesome icons）

### Apply to GitLab 行為

1. 點擊 "Apply to GitLab"
2. 顯示確認對話框：

   ```
   ⚠ 確定要套用排序到 GitLab？

   這會更新此 List 中所有 issues 的 relative position。
   此操作會影響其他使用者看到的排序。

   共 12 個 issues 將被更新。

   [取消] [確定套用]
   ```

3. 確認後批次呼叫 reorder API
4. 顯示進度 + 結果 toast

---

## Board 管理 UI

### BoardSelector

```
┌──────────────────────────────────────────────────────┐
│ Board: [My Sprint Board ▼]  [+ New]  [Manage]        │
└──────────────────────────────────────────────────────┘
```

（使用 Font Awesome icons: fa-plus, fa-cog）

### Board 下拉選單

```
┌─────────────────────────┐
│ ✓ My Sprint Board       │
│   Bug Triage Board      │
│   Feature Planning      │
├─────────────────────────┤
│   + Create new board    │
└─────────────────────────┘
```

（使用 fa-check, fa-plus）

### New Board 對話框

```
┌─────────────────────────────────────┐
│ Create New Board               ×   │
├─────────────────────────────────────┤
│ Name: [                    ]       │
│                                     │
│ Initial lists:                      │
│ ○ Empty (add lists later)          │
│ ○ From template:                    │
│   [To Do / In Progress / Done ▼]   │
│                                     │
│           [Cancel] [Create]         │
└─────────────────────────────────────┘
```

（使用 fa-times 關閉按鈕）

### Board 設定面板

```
┌─────────────────────────────────────────────────────┐
│ Board Settings: My Sprint Board                 ×  │
├─────────────────────────────────────────────────────┤
│ Name: [My Sprint Board        ]                    │
│                                                     │
│ Lists:                                 [+ Add List]│
│ ┌─────────────────────────────────────────────┐   │
│ │ ≡ To Do        Labels: backlog    [Edit][×] │   │
│ │ ≡ In Progress  Labels: doing      [Edit][×] │   │
│ │ ≡ Review       Labels: review     [Edit][×] │   │
│ │ ≡ Done         Labels: done       [Edit][×] │   │
│ └─────────────────────────────────────────────┘   │
│ (drag ≡ to reorder)                                │
│                                                     │
│ Special Lists:                                      │
│ [✓] Show "Others" list                             │
│ [✓] Show "Closed" list                             │
│                                                     │
│              [Delete Board] [Save Changes]          │
└─────────────────────────────────────────────────────┘
```

（使用 fa-grip-vertical 拖曳把手, fa-edit, fa-times, fa-plus, fa-trash）

### Edit List 對話框

```
┌─────────────────────────────────────┐
│ Edit List                      ×   │
├─────────────────────────────────────┤
│ Name: [In Progress         ]       │
│                                     │
│ Labels (issues must have ALL):     │
│ [doing                      ▼]     │
│  ☑ doing                           │
│  ☐ urgent                          │
│  ☐ bug                             │
│                                     │
│ Default Sort:                       │
│ [Position ▼]  [Ascending ▼]        │
│                                     │
│           [Cancel] [Save]           │
└─────────────────────────────────────┘
```

---

## 特殊 List 行為

### Others List

- 固定位置：最左邊
- 內容：不符合任何自定義 list 的 issues
- 可開關：透過 Board 設定
- 關閉時：這些 issues 完全隱藏

### Closed List

- 固定位置：最右邊
- 內容：state === 'closed' 的 issues
- 可開關：透過 Board 設定
- 關閉時：這些 issues 完全隱藏
- 拖曳進入：關閉 issue
- 拖曳離開：重新開啟 issue

---

## 實作階段

### 階段 1：建立共享資料層

- 建立 `GitLabDataContext`
- 建立 `GitLabWorkspace` 容器
- 重構 `GitLabGantt` → `GanttView`（保持功能不變）
- 確保現有 Gantt 功能正常

### 階段 2：建立 Kanban 基礎

- 建立 `KanbanView`, `KanbanBoard`, `KanbanList`, `KanbanCard`
- 實作 Board 顯示邏輯（根據 labels 分配 issues）
- 實作 ViewSwitcher 切換
- 雙擊開啟 Editor
- 新增 issue 功能

### 階段 3：Board 管理

- 建立 `useIssueBoard` hook
- 建立 `GitLabIssueBoardApi`（Snippet CRUD）
- 實作 BoardSelector
- 實作 Board 設定面板
- 實作 List 編輯對話框

### 階段 4：拖曳功能

- 整合 `@dnd-kit`
- 實作同 List 排序（reorder API）
- 實作跨 List 拖曳（label 切換）
- 實作 Others/Closed 特殊行為
- 實作樂觀更新 + 失敗回彈

### 階段 5：排序功能

- 實作 List 排序 UI
- 實作 Apply to GitLab 批次更新
- 實作確認對話框 + 警告

---

## 風險控制

- 每階段完成後確保現有 Gantt 功能正常
- 階段 1 是最大風險點（重構現有程式碼），需仔細測試
- 保持向後相容：`GitLabGantt` 作為 export alias 指向新的 `GitLabWorkspace`

---

## 技術依賴

新增套件：

- `@dnd-kit/core` - 拖曳功能
- `@dnd-kit/sortable` - 排序功能
- `uuid` - 產生 Board/List ID（如果尚未有）

---

## 決策記錄

| 問題                     | 決策                         | 理由                      |
| ------------------------ | ---------------------------- | ------------------------- |
| Kanban 與 Gantt 整合方式 | 獨立元件透過 shared context  | 避免 GitLabGantt 繼續膨脹 |
| 篩選器狀態               | 完全同步                     | 切換視圖時體驗一致        |
| Board 設定存儲           | GitLab Snippet               | 專案共享，跨裝置同步      |
| 拖曳同步策略             | 即時同步 + 樂觀更新          | 資料即時一致，失敗回彈    |
| Others/Closed 位置       | 固定（Others 左、Closed 右） | 簡化 UI，符合直覺         |
| 關閉特殊 list 時         | 完全隱藏 issues              | 簡潔，避免混淆            |
| 跨 List 拖曳 label 處理  | 只處理來源 list 的 label     | 符合直覺，保留其他 labels |
| List 排序                | 實際更新到 GitLab            | 需要警告確認              |
| Kanban Preset 內容       | 完整設定 + 預留擴充          | 支援未來需求              |
